import React from 'react';
import { RegistrationForm } from '../components';

const Registration = () => {
  return (
    <div>
      <RegistrationForm />
    </div>
  );
};

export default Registration;
